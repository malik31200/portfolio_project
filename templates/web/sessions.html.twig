{% extends 'base.html.twig' %}

{% block title %} Sessions disponibles - Actual Sport {% endblock %}

{% block body %}
    <h1 class="page-title">📅 Sessions disponibles</h1>
    <div class="filter-card">
        <h3>🔍 Filtrer les sessions</h3>
        <form method="get" action="{{ path('app_sessions') }}" class="filter-form">
            <div class="filter-row">
                <div class="filter-group">
                    <label for="course">Cours</label>
                    <select name="course" id="course" class="filter-select">
                        <option value="">Tous les cours</option>
                        {% for course in courses %}
                            <option value="{{ course.id }}" {% if selectedCourse == course.id %}selected{% endif %}>
                                {{ course.name }}
                            </option>
                        {% endfor %}
                    </select>
                </div>

                <div class="filter-group">
                    <label for="date">Date</label>
                    <input type="date" name="date" id="date" value="{{ selectedDate }}" class="filter-input">
                </div>

                <div class="filter-actions">
                    <button type="submit" class="btn btn-primary">Filter</button>
                    <a href="{{ path('app_sessions') }}" class="btn btn-secondary">Réinitialiser</a>
                </div>
            </div>
        </form>
    </div>

{% if sessions is empty %}
    <div class="card">
    <p>Aucune session disponible pour le moment.</p>
    </div>
{% else %}
    {% for session in sessions %}
        <div class="card">
            <div class="flex-between">
                <div>
                    <h3>{{ session.course.name }}</h3>
                    <p class="text-muted">{{ session.course.description }}</p>
                </div>
                <div class="badge-spots">
                    <div class="spots-number">{{ session.availableSpots }}</div>
                    <div class="spots-label">Places restantes</div>
                </div>
            </div>

            <div class="session-info">
                <div class="session-info-item">
                    <div class="info-label">📅 Date</div>
                    <strong>{{ session.startTime|date('d/m/Y') }}</strong>
                </div>
                <div class="session-info-item">
                    <div class="info-label">🕐 Horaire</div>
                    <strong>{{ session.startTime|date('H:i') }} - {{ session.endTime|date('H:i') }}</strong>
                </div>
                <div class="session-info-item">
                    <div class="info-label">⏱️ Durée</div>
                    <strong>{{ session.course.duration }} min</strong>
                </div>
                <div class="session-info-item">
                    <div class="info-label">💰 Prix</div>
                    <strong>{{ session.course.price }} €</strong> 
                </div>    
            </div>

            {% if app.user %}
                {% if session.availableSpots > 0 %}
                    <form method="post" action="{{ path('booking_reserve', {id: session.id}) }}">
                        <button type="submit" class="btn btn-full btn-success">
                            ✅ Réserver cette session
                        </button>
                    </form>
                {% else %}
                    <button disabled class="btn btn-full btn-disabled">
                        ❌ Complet
                    </button>
                {% endif %}
            {% else %}
                <a href="{{ path('app_login')}}" class="btn btn-full btn-primary">
                    🔐 Connectez-vous pour réserver
                </a>
            {% endif %}
        </div>
    {% endfor %}
{% endif %}
{% endblock %}